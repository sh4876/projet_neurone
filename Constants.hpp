#ifndef CONSTANTS_HPP
#define CONSTANTS_HPP

const int I(300); // l intensite du courant 
const int h(1000); // intervalle de temps en millisecondes
const int SimulationStart(0);
const int SimulationStop(100000);
const int tau(20); // time cst of the neurone
const double R(5); // la resistance
#endif
